openapi: 3.0.3
info:
  title: Header Params Fixture
  version: 1.0.0
paths:
  /users:
    get:
      operationId: listUsersWithHeader
      tags: [Header]
      parameters:
        - name: X-Request-Id
          in: header
          required: false
          schema:
            type: string
        - name: X-Client-Version
          in: header
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User list.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /users/{id}:
    delete:
      operationId: deleteUserWithHeader
      tags: [Header]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: X-Request-Id
          in: header
          required: false
          schema:
            type: string
      responses:
        "204":
          description: Deleted.